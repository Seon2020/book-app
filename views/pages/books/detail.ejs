<section>
    <% books.forEach(book => { %>
    <img src="<%= book.image_url %>">
    <h2><%= book.title %></h2>
    <h3><%= book.author %></h3>
    <p><%= book.isbn %></p>
    <p><%= book.description %></p>
    <a href="/">Navigate Home</a>
  <% }) %>
</section>