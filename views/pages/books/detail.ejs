<section>
    <% books.forEach(book => { %>
    <img src="<%= book.image_url %>">
    <h2><%= book.title %></h2>
    <h3><%= book.author %></h3>
    <p><%= book.description %></p>
    <p><%= book.isbn %></p>
    <p><%= book.bookshelf %></p>
    <a href="/">Navigate Home</a>
  <% }) %>
</section>